"use client";

// MobileDrawer.jsx — wraps Sidebar for the mobile overlay drawer.
// Same content, slightly different padding tweaks via parent class.

import { Sidebar } from "./Sidebar";

export function MobileDrawer() {
  return <Sidebar/>;
}
